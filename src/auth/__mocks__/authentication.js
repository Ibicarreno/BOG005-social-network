// eslint-disable-next-line no-unused-vars
export const createEmail = (email, password, nameUser) => Promise.resolve();

// eslint-disable-next-line no-unused-vars
export const validateUserAndPass = (email, password) => Promise.resolve();

export const loginWithGoogle = () => Promise.resolve();

// export { createEmail, validateUserAndPass, loginWithGoogle };
